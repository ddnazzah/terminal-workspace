node_modules
out
dist
release
*.tsbuildinfo
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea
.env
.env.local
.env.*.local

# Tailscale-serve certs for the mobile bridge (machine-local, contain a private key — never commit)
*.ts.net.crt
*.ts.net.key

# Stray production build output that lands at the repo root (regenerated by the build)
/index.html

# PWA scroll dev testbed (kept locally, not part of the shipped app)
/pwa/scroll-harness.html
/pwa/src/scroll-harness.ts
